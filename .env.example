# Personal access token for your personal GitHub account (read-only scopes are enough)
GH_STATS_TOKEN=
# Set to true to count private repos in the stats (default false). Only totals are published, never repo names
INCLUDE_PRIVATE_REPO=false
